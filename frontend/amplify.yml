version: 0.1
applications:
  frontend:
    phases:
      preBuild:
        commands:
          - yarn set version stable
          - yarn install
      build:
        commands:
          - yarn build
    artifacts:
      baseDirectory: ./dist
      files:
        - "**/*"
    cache:
      paths:
        - node_modules/**/*
