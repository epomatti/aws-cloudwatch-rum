version: 1
applications:
  - frontend:
    appRoot: frontend
    phases:
      preBuild:
        commands:
          - echo "HAHAHAHHAHAH"
          - ls
          - yarn set version stable
          - yarn install
      build:
        commands:
          - yarn build
    artifacts:
      baseDirectory: ./dist
      files:
        - "**/*"
    cache:
      paths:
        - node_modules/**/*
